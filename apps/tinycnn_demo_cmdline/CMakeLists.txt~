cmake_minimum_required(VERSION 3.2 FATAL_ERROR)
project(tinycnn_demo_cmdline)

# --------------------------------------
#  including external libraries
# --------------------------------------
include_directories(${TINYCNN_INCLUDE_DIR})
message (STATUS "TINYCNN_INCLUDE_DIR   :::   ${TINYCNN_INCLUDE_DIR}")



# we enable standard C++11 multithread support.
if( NOT WIN32 )
    #list(APPEND EXTRA_C_FLAGS -pthread)
    set(USE_PTHREAD ON)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -pthread")
    message(STATUS "TBB and OMP disabled: Using Pthread instead.")
else( NOT WIN32 )
    set(USE_PTHREAD OFF)
endif( NOT WIN32 )

# --------------------------------------
#  link external libraries
# --------------------------------------
link_directories(${TINYCNN_LIBRARY_DIRS})

add_executable( tinycnn_demo_cmdline tinycnn_demo_cmdline.cpp )

#target_link_libraries (tinycnn_demo_cmdline  ${project_library_target_name} ${REQUIRED_LIBRARIES}) 


